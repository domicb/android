<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="daw2.hlc.domi.mislugares" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VistaLugar"></activity>
        <activity
            android:name=".EdicionLugar"
            android:label="@string/title_activity_edicion_lugar"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <PreferenceFragment
            android:name=".Preferencias"
            android:label="Preferencias">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- ATTENTION: This data URL was auto-generated. We recommend that you use the HTTP scheme.
                  TODO: Change the host or pathPrefix as necessary. -->
                <data
                    android:host="mislugares.domi.hlc.daw2"
                    android:pathPrefix="/preferencias"
                    android:scheme="http" />
            </intent-filter>
        </PreferenceFragment>
    </application>

</manifest>
